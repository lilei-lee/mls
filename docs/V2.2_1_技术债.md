# V2.2 #1 技术债 & 代码异味清单

## Day 25 Bug Review 结论

| 坑 | 文件 | 结果 |
|---|---|---|
| 坑 56 · 协作 Tab 切换不刷新 | collaboration_list_screen.dart | ✅ 无问题 |
| 坑 50 · 卡片副标题 null | listing_list_screen.dart | ✅ 无问题 |
| 坑 59 · 状态变更后封面丢失 | listing_list_screen.dart | ✅ 无问题 |
| 坑 57 · "已通过中" 文案 | collaborations.py | ✅ 无问题 |
| Bug B · 编辑页双流程 | listing_edit_screen.dart | ✅ 无问题 |

## flutter analyze 现状

- 36 issues: 0 errors, 3 warnings, 33 info
- 全部 pre-existing，V2.2 未引入新 warning
- 3 个 warning: unused import(api_client.dart/photo_picker.dart) + unused _PlaceholderPage
- 建议 Day 33 顺手清掉

## 代码异味清单

### 1. listing_create_screen.dart — 长度 ~480 行

- `_build` 方法过长(~120 行)，3 个 section UI 可抽为独立 widget
- sale_points chip 逻辑与 listing_edit_screen.dart 近乎重复(~40 行)
- 建议: 抽 `SalePointsPicker` 共享 widget

### 2. listing_edit_screen.dart — 长度 ~600 行

- sale_points 辅助函数( toggle/add/remove)与创建页重复
- `_BuildSyncBanner` 可作为独立 widget 文件
- 建议: 抽 `SalePointsPicker`、`ObjectiveFeaturePicker` 共享 widget

### 3. listing_detail_screen.dart — 长度 ~500 行

- `_buildCommunityMiniCard` 逻辑较复杂(~35 行)，可抽为独立 widget
- 建议: 抽 `CommunityMiniCard` widget (Day 35-36 社区详情增强时会用)

### 4. listing_shared_screen.dart — 长度 ~500 行

- `_SharedListingCard` 内部方法较多，可考虑拆为独立 widget
- 筛选状态 chip 与 FilterSheet 逻辑耦合度高

### 5. 后端 listings.py — 长度 ~1200 行

- 函数众多，部分可拆到独立 service 文件
- `_enrich_from_dictionary` / `_enrich_community_from_dict` / `_enrich_physical_fields` 三个富化函数逻辑重复

### 优先级

| 优先级 | 项目 | 建议 |
|---|---|---|
| 低 | 抽共享 widget | Day 33 顺手做 |
| 低 | 清 flutter analyze warnings | Day 33 顺手清 |
| 中 | 后端 listings.py 拆分 | V2.3 重构 |
