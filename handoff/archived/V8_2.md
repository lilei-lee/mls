# MLS 交接文档 V8.2(长期档)

> **更新时间**:Day 16 末(2026-04-29 中午)
> **承接**:V8.1 长期档(Day 15 末状态)
> **本档新增**:Day 16 · 6 commit 全收官 · 4 新坑 · 路由单复数命名表更新 · Day 17 锚点改"种子用户实战 + 修反馈 bug"
> **下次开工锚点**:Day 17 · 拉 1-2 个真实经纪人朋友,陪他们走完整流程,边走边修

---

## 一、人 / 项目基本信息(承袭 V8.1)

- **磊**:创始人 + 唯一开发者,张家口,Windows 11 笔记本,非技术背景
- **项目路径**:`C:\projects\mls\`
- **栈**:Flutter 3.41.7 + FastAPI + MongoDB 8.2 + fakeredis + APScheduler 3.11.2 + JWT
- **当前 IP**:重启后查 ipconfig 确认,可能变化
- **测试账号**:张三 13912345678 (LA) / 李红 13200132000 (BA)
- **Swagger UI**:http://<ip>:8000/docs

---

## 二、Day 10-16 战绩(累计 21 个 commit)

### Day 10-15(承袭 V8.1,简列)

- **Day 10**:客户管理后端 4 commit
- **Day 11**:5 Tab 骨架 + 工作台重写 3 commit
- **Day 12**:客户 Tab 全套 1 commit
- **Day 13**:协作 Tab + 进度条 1 commit
- **Day 14**:5 Tab 全部真实化 1 commit
- **Day 15**:1:N 带看专项重构 + 退出登录 4 commit

### Day 16 · bug 清理 + 客户选择器 + 直接带看入口(6 commit)🆕

```
4c101a3  fix(Day16): 协作卡跳成交详情路径单复数对齐
xxxxxx1  feat(Day16): 申请流程加客户选择器 · 选老客户带入三字段
xxxxxx2  fix(Day16): 申请页选老客户后只锁身份字段 · 需求允许本次微调
xxxxxx3  feat(Day16): 直接带看(1:N)前端入口 · 协作详情加再次带看按钮
xxxxxx4  fix(Day16): 直接带看 doc 字段补齐 · _format 容错 · 修 LA 拉详情 500
4beca79  fix(Day16): 状态码本地化补齐 · 修带看待确认 key 错位
e6ace8f  feat(Day16): 共享库卡片显示我的申请状态 · 防重复申请
```

新增/修改:
- **`lib/screens/collaboration_list_screen.dart`**:`_onTap` 跳 transaction 详情路径 `/transactions/` → `/transaction/` (单数,跟路由声明对齐)
- **`lib/services/showing_request_service.dart`**:`create()` 加可选 `customerId` 参数,有就透传给后端
- **`lib/screens/showing_request_create_screen.dart`**:重写 · 加"客户来源"区块(从我的客户选 / 本次新建) + BottomSheet 客户选择器 + 选中后只锁姓氏/性别(身份属性),需求始终可编辑
- **新增 `lib/screens/direct_showing_create_screen.dart`**:直接带看(1:N)新建页,客户和房源信息只读展示,只填带看时间+照片+备注
- **`lib/router/app_router.dart`**:加 `/showings/direct/new` 路由
- **`lib/screens/showing_request_detail_screen.dart`**:带看记录区 confirmed 分支加"再次带看(无需重新审批)"按钮 + 加 `_goDirectShowing` 方法
- **`lib/services/showing_service.dart`**:加 `createDirect()` 方法
- **`lib/widgets/status_labels.dart`**:修 `_showing` 字典首条 key 从 `pending_la_confirm` → `pending_confirm`(关键修复) + `_showingRequest` 加 `auto_approved` / `merged_into_prior`
- **`backend/showings.py`**:`_format_showing` / `_format_showing_lite` 全字段 `.get()` 容错 + `_iso_or_none` 助手 + 加 1:N 留痕字段(is_repeat_showing / original_request_id)透出
- **`backend/customers.py`**:`create_direct_showing` doc 字段集对齐 `submit_showing`,补 `ba_agent_name` / `ba_agent_phone` / `la_agent_name` / `ba_submitted_at` / `la_reviewed_at` / `listing_cycle`
- **`backend/listings.py`**:`list_shared_listings` 加批量 join `showing_requests`,每条 listing 挂 `my_request_status`(pending/approved/null) + 新增 `_build_my_request_status_map` 助手
- **`lib/screens/listing_shared_screen.dart`**:`_SharedListingCard` 标题右侧加状态标签(橙"已申请" / 绿"已通过") + 申请按钮文案 + 颜色随状态变 + 已申请时点击拦截给 SnackBar 提示

---

## 三、当前 5 Tab 状态(全真实化 + 防重复申请)

| Tab | 内容 | 来源 |
|---|---|---|
| 工作台 | 张三好 + 待办 + 24h 动态 + 4 宫格快速入口 + 头像退出登录 | Day 11/15 |
| 房源 | 我的 / 共享库 切换 + 筛选 + **共享库卡片显示我的申请状态(Day 16)** | Day 6 + Day 16 |
| 协作 | 买方/卖方 TabBar + 进度条卡片 + **再次带看入口(Day 16)** | Day 13/15/16 |
| 客户 | 客户列表 + 新建/详情 + 时间线(过滤已归并)+ **作为申请页客户选择器数据源(Day 16)** | Day 12/15/16 |
| 奖金 | 待操作奖金单 + 卡片展示 | Day 8 老页 |

工作台头像 → 弹底部菜单(只有"退出登录")

---

## 四、🟢 Day 16 已修 bug 清单(全部通过验收)

| Bug | 当时症状 | 根因 | 修法 |
|---|---|---|---|
| 协作卡片 404 | 点付款中卡跳 `/settlement/{id}` 404 | `/settlement/` 单数错写 | 改 `/settlements/`(V8.1 已记) |
| 协作卡片 404(第二条) | 点华新园已完成卡跳 `/transactions/{id}` 404 | router 声明 `/transaction/:id`(单数),跳转写复数 | 跳转改单数对齐 |
| 申请页选老客户死锁 | 老客户档案没填需求,选了老客户后需求字段被锁,无法填,提交校验又必须填 | 我代码里"选老客户=三字段全锁"过严 | 只锁身份(姓氏+性别),需求始终可编辑 |
| 直接带看返回值崩 | 后端 200 + 数据已写,前端解析 `Null is not a subtype of String` | service 抄 submit() 平铺结构,实际返 `{success,data:{...}}` 嵌套 | 改 `resp.data['data']['showing_id']` |
| LA 拉直接带看详情 500 | KeyError: 'ba_submitted_at' | Day 15 `create_direct_showing` 写库时漏字段 | A. formatter 全字段 .get 容错 + B. 补齐 doc 字段 + 数据库回填脚本 |
| 状态码英文显露 | 待确认带看 chip 显示 `pending_confirm` 英文 | `StatusLabels._showing` 字典首条 key 从一开始就写错(写成成交那边的 `pending_la_confirm`),颜色对了所以一直没暴露 | 修字典 key + 顺手补 auto_approved/merged_into_prior |

---

## 五、当前已知 bug 清单(Day 17 头一件事或登债)

无确认的功能性 bug。Day 16 末验收 6 条流程全过。

---

## 六、Day 17 待办(种子用户实战)

### 6.1 拉真人走流程(估 1-2 小时,加上心理准备时间)

V8.1 第十一节早就给了清单,Day 17 真做:

- [ ] 找 1-2 个真实经纪人朋友(微信认识的同行)
- [ ] 准备一份"操作手册 v1"(可选):1-2 页 PDF / 微信图文,讲基本玩法
- [ ] 现场陪同他们走一遍:**注册 → 录房 → 看共享库 → 申请带客 → 审批 → 提交带看 → LA 确认 → (可选)发起成交确认 + 标已付款**
- [ ] 让他们用自己的真实房源,真实客户姓氏(可匿名),记录他们卡在哪、看不懂哪、想要什么没有

**为什么是 Day 17 头号大事**:Day 10-16 都是磊 + Claude 闭门写,从来没有真用户测过。1-2 个真用户半小时反馈,顶 7 天闭门猜。

### 6.2 听到反馈立刻分流

| 反馈类别 | 处理 |
|---|---|
| 流程不通 / 操作崩 | **当场修**(优先级最高) |
| 信息看不懂(状态码、文案) | Day 17 当晚清掉 |
| 想要新功能 | 登债到 V8.3,**不当场加**(避免无序) |
| 表扬 / 顺畅完成 | 记下来,Day 17 末发个朋友圈 :) |

### 6.3 Day 17 末复盘 + 决定 V2 节奏

种子反馈跑完 → 决定:
- 反馈基本顺畅 → V2 收官,转 V2.1 边迭代边拉用户
- 反馈卡点多 → V2 延期,集中修一周再拉

---

## 七、技术债清单(V8.1 升级版)

### 🔴 高优先级(种子上线后立刻补)

- [PENDING] **B 对象存储迁移**(3-4 小时):照片 base64 存 MongoDB 是 50 户内的临时方案,种子用户产生真实数据后必须迁。需先申请腾讯/阿里/七牛 COS 账号
- [PENDING] **🆕 历史"无 customer_id"的协作回填**(Day 16 末发现):Day 12 之前的协作里 customer 是手填,没进 customers 集合。要写一次性脚本扫历史 showing_requests 里 customer_id=null 的条目,按 ba_agent_id + customer_surname + customer_gender 归并到客户档案。**种子用户上线之前必做**,否则客户选择器看不到老协作的客户

### 🟡 中优先级(3-5 天内)

- [PENDING] **🆕 直接带看(1:N)的 listing 状态守卫过严**(Day 16 验收发现):后端 `create_direct_showing` 当前只允许 listing 在 on_sale/deposit_paid 时再次带看,但业务上 transaction_ongoing 也有合理"陪家人复看"场景。修法:`backend/customers.py` `create_direct_showing` 把 transaction_ongoing 加进白名单
- [PENDING] **房源表单缺奖金输入字段**(Day 8 暴露,V8.1 没修):listing_create_screen.dart / listing_edit_screen.dart 加 TextField + 服务层透传
- [PENDING] **成交日期 vs 带看时间严格比较 bug**(Day 8 暴露)
- [PENDING] **initiate_transaction 未给 bonus_yuan 拍快照**(Day 8 暴露)
- [PENDING] **共享库卡片不显示奖金标签**(Day 8 暴露,Day 16 已部分:实际 `_format_listing_anonymous_lite` 是返了 `bonus_yuan` 字段的,前端卡片左上角已显示橙色"奖¥xxx" — 此条可能已无效,Day 17 复检)
- [PENDING] **带客申请 7 天过期定时任务**(45 分钟):APScheduler,不依赖推送

### 🟢 低优先级(50 户以内不做)

- [PENDING] **🆕 状态码命名风格不统一**(Day 16 发现):`pending_confirm`(带看) vs `pending_la_confirm`(成交) —— 都是"等 LA 处理",不同前缀容易抄串,导致 `StatusLabels._showing` 字典 key 一开始就写错。建议**保留现状不动**(改后端要回填历史),所有新建状态机一律遵循 `<actor>_<action>` 命名,如 `la_confirm_pending`、`ba_payment_pending`,避免再加歧义 key
- [PENDING] **🆕 dashboard_summary 被单次会话重复请求**(V7.2 已记)
- [PENDING] 状态码本地化:V7.2 提到的 deposit_paid/transaction_ongoing/sold —— Day 16 status_labels.dart 已经覆盖了,但前端还有零散硬编码地方在显英文,真出现一处修一处即可
- [PENDING] 日期/时间选择器默认英文 OK / CANCEL:`main.dart` 加 `localizationsDelegates` + `supportedLocales: [Locale('zh','CN')]`
- [PENDING] LA 催促 / 14 天回退(等推送)
- [PENDING] 驳回 2 次冻结(等推送)
- [PENDING] 30 天修正窗口
- [PENDING] 自促成交分支(LA=BA)
- [PENDING] 无定金直接签约
- [PENDING] 后台代确认 / 争议仲裁
- [PENDING] 奖金结算 BA 确认收款 + 凭证(等 B)
- [PENDING] 其他 5 个房源状态:coming_soon/paused/pending_check/archived/locked
- [PENDING] DOM 计算 + 自动转状态定时任务
- [PENDING] 归属变更流程
- [PENDING] 开放看房日 / 收藏房源 / 共享库卡片点击进详情

### 🟣 迁移相关

- [PENDING] 上 git 远程版本管理(Github/Gitee)
- [PENDING] 前端 baseUrl 做 dotenv 配置
- [PENDING] 生产 MongoDB 迁云

---

## 八、🔑 路由命名速查表(Day 16 末更新)

**4 个业务 4 套命名,基本无规律。新增跳转代码前必查此表。**

| 业务 | 列表/特殊页 | 详情页 |
|---|---|---|
| listing | `/listings/mine`(复数) `/listings/shared`(复数) `/listing/new`(单数) | `/listing/:id`(**单数**) `/listing/:id/edit` |
| showing-request | `/showing-requests/sent`(复数+中划线) `/showing-requests/received`(复数+中划线) `/showing-request/new`(单数+中划线) | `/showing-request/:id`(**单数+中划线**) |
| showing | `/showings/pending-confirm`(复数) `/showings/direct/new`(复数,Day 16 新) | `/showing/:id/confirm`(**单数+confirm**) `/showing/submit` |
| transaction | `/transactions/pending-la`(复数) | `/transaction/:id`(**单数**) `/transaction/initiate` |
| settlement | `/settlements/pending-my`(复数) | `/settlements/:id`(**复数**)|
| customer | `/customer/new` `/customer/:id`(单数,Day 12 干净一致) | 同左 |

**踩坑频次**:transaction 详情(Day 7 单数,Day 13 跳转写复数,Day 15 末暴露,Day 16 修)。**未来若改命名规范,要全文搜替换 + 全链路回归**。

---

## 九、🔑 坑位档(累计到 Day 16)

V8.1 已记坑 1-27。**Day 16 新增坑 28-31**:

### 🆕 坑 28(Day 16):新接口返回结构嵌套,前端抄旁边方法的脑型崩

`/showings/direct` 返 `{success: true, data: {showing_id: ...}}`(嵌套),前端 `showing_service.createDirect` 抄 `submit()` 的 `resp.data['showing_id']`(扁平)导致 `Null is not a subtype of String` 崩。**经验**:每写新 service 方法前,先 Swagger 试一次接口,看清返回结构再写解析,不能抄旁边方法的脑型。

### 🆕 坑 29(Day 16):跨模块新写 doc 时字段集对照不全

`customers.py:create_direct_showing` 写新 showing 时,字段集没对齐 `showings.py:submit_showing` 的全集,漏 `ba_submitted_at` / `ba_agent_name` 等,导致 `_format_showing` 在 LA 拉详情时 `KeyError` 500。**经验**:跨模块新写 doc 前,先用 `db['集合'].find_one({}, sort=[('created_at',-1)])` 拉一条已有的对照字段全集,再起手写新 insert_one;formatter 一律 `.get()` 容错。

### 🆕 坑 30(Day 16):状态码命名前缀风格不统一,字典 key 抄错难发现

带看是 `pending_confirm`(无 la 前缀),成交是 `pending_la_confirm`(有 la 前缀),都是"等 LA 处理"。`StatusLabels._showing` 字典首条 key 一开始就把成交那边的 key 抄过来了,因颜色映射对了所以历史上一直没暴露,Day 16 直接带看撞上才发现。**经验**:写状态码字典时,别脑补 key,用 `db.集合.distinct('status')` 列出真实存在的所有 key 再一一映射。

### 🆕 坑 31(Day 16):整替整文件后必须冷启,大写 R 不刷新 widget tree 结构

整替 `showing_request_create_screen.dart` 加客户来源区块后,真机大写 R 热重启没刷新到新结构,表现是"按钮位置看起来没变"。**经验**:**整替整文件 = 一定 q + flutter run**,涉及新加方法 / 新加成员变量 / 新引入子组件时尤其。`r`(小写)只刷 build,`R`(大写)只刷 widget,只有冷启会重建 dart isolate。

---

## 十、新增 / 修改文件汇总(Day 16)

### 新增

```
app/mls_app/lib/screens/direct_showing_create_screen.dart  (Day 16)
```

### 修改

```
backend/listings.py                                    (Day 16:my_request_status 批量 join)
backend/showings.py                                    (Day 16:formatter 全字段 .get 容错)
backend/customers.py                                   (Day 16:create_direct_showing doc 补字段)

app/mls_app/lib/router/app_router.dart                 (Day 16:+ /showings/direct/new)
app/mls_app/lib/widgets/status_labels.dart             (Day 16:修 _showing 字典 + 补申请字典)
app/mls_app/lib/services/showing_service.dart          (Day 16:+ createDirect 方法)
app/mls_app/lib/services/showing_request_service.dart  (Day 16:create + customer_id 参数)
app/mls_app/lib/screens/collaboration_list_screen.dart (Day 16:transaction 跳转单数对齐)
app/mls_app/lib/screens/showing_request_create_screen.dart (Day 16:整替 · 加客户选择器)
app/mls_app/lib/screens/showing_request_detail_screen.dart (Day 16:+ 再次带看按钮 + 方法)
app/mls_app/lib/screens/listing_shared_screen.dart     (Day 16:卡片标签 + 按钮状态化)
```

---

## 十一、下次开工锚点(Day 17)

### 1 · 起服务老三样

```cmd
ipconfig                 # 查 IP,如有变化改 api_config.dart

cd C:\projects\mls\backend
venv\Scripts\activate
uvicorn main:app --reload --host 0.0.0.0 --port 8000
# 期望:8 ✓ + 定时任务调度器已启动 + Application startup complete

# 新开 cmd
cd C:\projects\mls\app\mls_app
flutter run
# 真机张三或李红登录,5 Tab 全部能用,共享库卡片右上角能看到状态标签
```

### 2 · 第一句开工密码

> "继续 Day 17:种子用户实战。读 V8.2 交接档第六节(Day 17 待办)+ 第七节(技术债 🔴 高优先级)。**不写代码**,先做"历史无 customer_id 协作回填"脚本(技术债 🔴 第二条),再准备拉 1-2 个真实经纪人朋友陪走流程。流程中冒出来的 bug 当场修,信息看不懂的当晚清,新功能登债。开始吧。"

### 3 · 当天 KPI

- ✅ 历史协作 customer_id 回填脚本跑通(必须,否则种子上线后老数据看着混乱)
- ✅ 拉到 1 个真实经纪人朋友陪走完整流程(必须)
- 🎯 修掉对方碰到的所有"流程崩"级 bug(冲刺)
- 🚀 当晚朋友圈发"MLS 第 N 个种子用户跑通了"(收尾庆祝)

---

## 十二、V2 完成度估算

```
✅ Day 10-14:数据模型 + 5 Tab 真实化 + 工作台 / 协作 / 客户全套
✅ Day 15:1:N 带看重构 + DB 迁移 + 退出登录入口
✅ Day 16:bug 清理 + 客户选择器 + 直接带看入口 + 共享库防重复
🎯 Day 17:种子用户实战 + 真反馈 → 真修 bug
🚀 Day 18+:转"反馈 → 修 bug → 小迭代"模式,可考虑迁 Claude Code 加速
```

**完成度**:**约 94%**

剩余 6% = 历史协作 customer_id 回填(必做)+ B 对象存储迁移(必做)+ 零碎技术债

---

## 十三、Day 16 关键经验(避免重复踩)

### 经验 1:字段集对照,新写不能拍脑袋

Day 15 写直接带看时凭空写 doc 字段集,跟 submit_showing 对不上,Day 16 LA 拉详情就 500。后果:用户看到红色 Page Not Found / DioException。**通用规则**:写新 insert_one 之前,**先 find_one 拉一条已有的对照样本**,再起手。formatter 一律 `.get` 容错,等于做最后一道防线。

### 经验 2:验证步骤要跨业务身份

Day 13/15 写协作跳转和直接带看时,只用 BA 视角验收过,LA 那边的 bug 都是 Day 16 才暴露(协作卡 transaction 详情 404 / LA 拉直接带看详情 500)。**经验**:任何涉及"LA-BA 双视角"的功能,**两个角色都要切到验收一遍**,最好登一份"双视角验收清单"放协作规范里。

### 经验 3:大写 R 是骗子,改文件结构必须 q + flutter run

V8.1 到 Day 16 各自踩了一次。改成员变量 / 新增方法 / 引入子组件 → 大写 R 不重建 widget tree。**默认动作**:整替了任何文件,直接 q 退出再 flutter run,不要试图省那 30 秒。

### 经验 4:今天遇到的 bug 不是孤例,要抽象出"未来如何避免"

Day 16 的 4 个新坑都是这种性质 —— 不是"这个 bug 修了"就完事,而是"未来同类 bug 怎么不再出"。所以坑位档每条都带"经验"字段,这是磊半年攒下来最值钱的资产之一。

---

## 十四、协作约定(承袭 V8.1)

- **代码交付**:长文件分 Batch 给,整替 = Ctrl+A → Delete → Ctrl+V → Ctrl+End → Ctrl+S → q + flutter run(冷启)
- **服务名一致性**:前端 service 里的路径是相对路径(`/showings`、`/transactions`),不写 `/api/v1`(已在 baseUrl 包含)
- **FastAPI 路由顺序**:具体路径先于 `{id}` 注册
- **Pydantic 三处必改口诀**:加字段 → 改 doc / Create / Update 三个地方
- **viewer-aware 格式化器**:涉及双方博弈的集合,详情接口必须接 `viewer_id`,返 `viewer_role: "la" | "ba" | null`,前端读这个判身份不靠姓名兜底

---

## 十五、常用命令速查(承袭 V8.1)

```cmd
REM 后端启动
cd C:\projects\mls\backend && venv\Scripts\activate && uvicorn main:app --reload --host 0.0.0.0 --port 8000

REM 前端启动
cd C:\projects\mls\app\mls_app && flutter run

REM 查 IP
ipconfig

REM 杀残余 Java(Gradle 锁)
taskkill /F /IM java.exe

REM 看 listings 全表
cd C:\projects\mls\backend && venv\Scripts\activate && python -c "from database import db; [print(d.get('community'), d.get('building'), d.get('room_no'), '| status=', d.get('status')) for d in db['listings'].find({},{'community':1,'building':1,'room_no':1,'status':1})]"

REM 看某条 showing 全字段(诊断用)
python -c "from database import db; from bson import ObjectId; sh = db['showings'].find_one({'_id': ObjectId('xxxxx')}); [print(k, '=', v) for k,v in sh.items()]"
```

---

**文档版本**:V8.2(Day 16 末状态)
**作者**:磊 + Claude
**适用**:下次开工新 Claude 对话首次贴入,快速接力
